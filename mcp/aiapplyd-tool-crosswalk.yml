generated: '2026-09-23'
method: derived
source:
- mcp/aiapplyd-mcp-tools-list.json
- openapi/aiapplyd-openapi.yml
note: The published OpenAPI carries no operationIds (0 of 372 operations), so REST bindings are recorded as METHOD
  + path, matched by name and semantics.
surfaces:
  openapi: openapi/aiapplyd-openapi.yml
  mcp: https://mcp.aiapplyd.com/mcp (tools/list anonymous; calls OAuth-gated)
  graphql: null
crosswalk:
- tool: aiapplyd_score_resume
  category: scoring
  rest:
  - POST /ats/score
  binding: rest
  confidence: high
- tool: aiapplyd_optimize_resume
  category: resume
  rest:
  - POST /ats/optimize
  binding: rest
  confidence: high
- tool: aiapplyd_generate_interview_questions
  category: interview
  rest:
  - POST /jobs/{id}/generate-interview-prep
  binding: rest
  confidence: low
  note: REST route is keyed to a stored job; the tool takes free-text title/company/description.
- tool: aiapplyd_translate_resume
  category: resume
  rest:
  - POST /user-documents/translate-preview
  - POST /user-documents/{id}/translate
  binding: rest
  confidence: medium
- tool: aiapplyd_search_jobs
  category: jobs
  rest:
  - GET /job-matches
  binding: rest
  confidence: medium
  note: Tool searches the curated match feed; REST lists the signed-in user's job matches.
- tool: aiapplyd_update_job_preferences
  category: jobs
  rest:
  - PUT /job-matches/preferences
  binding: rest
  confidence: high
- tool: aiapplyd_generate_cover_letter
  category: cover-letter
  rest:
  - POST /resume-builder/{id}/generate-cover-letter
  binding: rest
  confidence: low
  note: REST route is keyed to a resume-builder document; the tool takes a job description and company name.
mcp_only:
- tool: aiapplyd_analyze_job_description
  reason: No REST route in the published contract analyzes a free-text job description on its own.
- tool: aiapplyd_auto_apply
  reason: Applications are created by the product pipeline; no public REST route accepts a job URL to apply to.
- tool: aiapplyd_build_pdf
  reason: No REST route in the contract takes resume text and a template and returns a PDF.
rest_only: []
coverage:
  tools: 10
  bound: 7
  mcp_only: 3
  rest_operations: 372
rest_only_note: the remaining operations of the 372 in the contract (account, billing, applications, admin, internal,
  webhooks) have no tool; not enumerated
